#!/sbin/openrc-run
# OpenRC init script for power_herald_processor

description="Power Herald State Processor Daemon"
command="/opt/power_herald/venv/bin/python"
command_args="-m src.processor"
pidfile="/var/run/power_herald_processor.pid"
user="power_herald"
group="power_herald"
directory="/opt/power_herald"

depend() {
    need net
}

start_pre() {
    if [ ! -d "$directory" ]; then
        eerror "Directory $directory does not exist"
        return 1
    fi
}

start() {
    ebegin "Starting Power Herald State Processor"
    cd "$directory"
    start-stop-daemon --start --pidfile "$pidfile" \
        --user "$user" --group "$group" \
        --make-pidfile --background \
        --exec "$command" -- $command_args
    eend $?
}

stop() {
    ebegin "Stopping Power Herald State Processor"
    start-stop-daemon --stop --pidfile "$pidfile"
    eend $?
}